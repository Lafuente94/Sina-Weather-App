services:
  - type: web
    name: meteo-soria-api
    runtime: python
    plan: free
    rootDir: src
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT